<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <li>
    <%= link_to "#{task.title} - completed: #{task.completed ? "yes" : "no"}", task_path(task) %>
  </li>
<% end %>

<hr>

<%= link_to "Add a task", new_task_path %>